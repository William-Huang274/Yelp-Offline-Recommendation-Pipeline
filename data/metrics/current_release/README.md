# Current Release Metrics Surface

This directory contains compact metrics snapshots used by the current release
story.

It is meant for documentation and repository review, not for raw experiment
replay. Source-of-truth frozen artifacts remain under:

- `../../output/_prod_runs/`
