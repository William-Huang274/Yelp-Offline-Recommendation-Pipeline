# Public CPU-friendly baseline for smoke tests, validators, and non-GPU stages.
numpy>=1.26
pandas>=2.2
pyarrow>=14
scipy>=1.10
scikit-learn>=1.4
xgboost>=2.0
pyspark>=3.5
pytest>=8,<9
# SSH/SFTP helper for Stage11 cloud inventory and explicit artifact pulls.
paramiko>=3.4

# Optional GPU-heavy dependencies for the current Stage11 Qwen3.5-9B
# reward-model training workflow and prompt-only probe runs remain in:
# requirements-stage11-qlora.txt
