[pytest]
testpaths = tests
norecursedirs = tests/archive

# The active smoke suite targets the current public repository surface.
# Legacy tests remain under tests/archive and are intentionally excluded.
