# GPU-oriented extras used by the current Stage11 reward-model mainline and by
# prompt-only / persona-SFT probe runs when a GPU environment is available.
# The current frozen reward-model mainline uses Qwen3.5-9B.
# Optional prompt-only probe templates in docs/stage11 target
# Qwen3.5-35B-A3B / Qwen3-30B-A3B cloud experiments.
# These packages are not required for the public CPU smoke surface.

torch>=2.1
transformers>=4.40
accelerate>=0.29
peft>=0.11
datasets>=2.18
sentencepiece>=0.1.99
safetensors>=0.4
scipy>=1.10

# Optional but recommended for 4-bit QLoRA (GPU only).
bitsandbytes>=0.43
