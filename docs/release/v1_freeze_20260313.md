# V1 Freeze Note (2026-03-13)

## 1. Purpose

This document records the `v1` freeze completed on `2026-03-13` based on the
latest successful cloud-side results available at that time.

The goal of this freeze is not to declare the project fully production-ready.
The goal is to pin the most important `stage09 -> stage11` mainline and land the
corresponding artifacts locally as a fixed baseline for later closeout, repair,
and evaluation alignment.

Freeze timestamp:

- `2026-03-13 18:52:52 +08:00`

Machine-readable manifest:

- `data/output/_v1_freeze_20260313/v1_freeze_manifest.json`

## 2. Freeze Conclusion

This `v1` freeze locks in two judgments at the same time:

1. the safe default release path still remains `stage09 + PreScore@10`
2. the best frozen cloud-side experimental result is currently the
   `DPO sidecar` result tied to `20260313_151256 stage11_3`

The reasoning is direct:

- the latest cloud evaluation shows `QLoRASidecar@10` clearly outperforming
  `PreScore@10` under the same evaluation batch
- but `GL-07` is still incomplete, so `stage10` and `stage11` are not yet fully
  aligned on a completely unified release cohort

So the correct interpretation of this freeze is:

- `the v1 freeze is complete`
- `the final internal champion has not yet been formally declared`

## 3. Freeze Lineage

The frozen cloud-side lineage is:

- `stage09 source`
  - `/root/autodl-tmp/stage09_fs/20260311_005450_full_stage09_candidate_fusion`
- `stage11_1`
  - `/root/autodl-tmp/stage11_fs/input11data/20260311_011112_stage11_1_qlora_build_dataset`
- `stage11_2 SFT`
  - `/root/autodl-tmp/stage11_fs/models/20260312_175852_stage11_2_qlora_train`
- `stage11_2 DPO eval run`
  - `/root/autodl-tmp/stage11_fs/models/20260312_221630_stage11_2_dpo_train_9b_v3full_ckpt485_eval_run`
- `stage11_3 latest eval`
  - `/root/autodl-tmp/stage11_fs/sidecar_eval/20260313_151256_stage11_3_qlora_sidecar_eval`

After rechecking the cloud-side latest pointers, the lineage above still holds.

## 4. Why This Source Was Chosen Instead Of A Newer Stage09 Run

A newer cloud-side `stage09` directory exists:

- `/root/autodl-tmp/stage09_fs/20260313_170846_full_stage09_candidate_fusion`

However, during the `2026-03-13` freeze validation, that directory was still
empty. This freeze therefore excludes it explicitly and uses the latest
successful run with a complete lineage instead.

The rule for this `v1` freeze is:

- freeze the latest successful result with a complete lineage
- do not freeze a directory that only has a newer name but empty contents

## 5. Local Materialized Directories

The following directories have already been landed locally:

- `D:\5006_BDA_project\data\output\09_candidate_fusion\20260311_005450_full_stage09_candidate_fusion`
- `D:\5006_BDA_project\data\output\11_qlora_data\20260311_011112_stage11_1_qlora_build_dataset`
- `D:\5006_BDA_project\data\output\11_qlora_models\20260312_175852_stage11_2_qlora_train`
- `D:\5006_BDA_project\data\output\11_qlora_models\20260312_221630_stage11_2_dpo_train_9b_v3full_ckpt485_eval_run`
- `D:\5006_BDA_project\data\output\11_qlora_sidecar_eval\20260313_151256_stage11_3_qlora_sidecar_eval`

Materialized size summary:

- `stage09`: `107` files, `236,501,072` bytes
- `stage11_1`: `85` files, `166,853,117` bytes
- `stage11_2 SFT`: `9` files, `136,443,017` bytes
- `stage11_2 DPO eval run`: `12` files, `194,911,169` bytes
- `stage11_3`: `6` files, `16,538,641` bytes

Notes:

- the `stage09` local sync already materialized the cloud symlink target into
  real local contents
- the `stage11_2 SFT` sync intentionally keeps only the adapter and essential
  audit files
- the full `trainer_output/checkpoint-*` directory tree was not copied locally

## 6. Freeze Metrics

### 6.1 Existing Local Stage09 Audit Baseline

The stage09 audit baseline currently retained in the repository is still:

- `data/metrics/stage09_recall_audit_summary_latest.csv`

Recorded values:

- `run_id = 20260305_003539`
- `source_run_09 = 20260305_002746_full_stage09_candidate_fusion`
- `bucket = 10`
- `truth_in_all = 0.947208`
- `truth_in_pretrim = 0.882255`
- `hard_miss = 0.052792`

This means the formal local stage09 audit file still points to an older source.
That is exactly why `GL-06` remains a follow-up priority.

### 6.2 Latest Frozen Stage11 Result

Source:

- `data/output/11_qlora_sidecar_eval/20260313_151256_stage11_3_qlora_sidecar_eval/qlora_sidecar_metrics.csv`

Aligned evaluation result:

- `PreScore@10`
  - `recall = 0.056910569105691054`
  - `ndcg = 0.02646705876884783`
- `QLoRASidecar@10`
  - `recall = 0.06775067750677506`
  - `ndcg = 0.02993468209586097`

Observed gain:

- `recall +0.010840108401084008`
- `relative recall uplift +19.047619%`
- `ndcg +0.00346762332701314`
- `relative ndcg uplift +13.101657%`

Evaluation scale:

- `n_users = 738`
- `n_items = 1451`
- `n_candidates = 184500`

### 6.3 Alpha Sweep

The freeze also keeps:

- `data/output/11_qlora_sidecar_eval/20260313_151256_stage11_3_qlora_sidecar_eval/alpha_sweep_best.json`

Best recorded value:

- `alpha = 0.54`
- `recall@10 = 0.06775067750677506`
- `ndcg@10 = 0.03012705082592172`

Notes:

- `run_meta` still records the default `blend_alpha = 0.5`
- but the sweep result inside the same run reached the best score at `alpha = 0.54`
- this freeze preserves the fact pattern and does not change the current script
  default

## 7. What This Freeze Does Not Resolve

This freeze does not resolve the following issues:

- `GL-01`: cloud credentials were not rotated yet
- `GL-06`: the `20260311_005450` stage09 run has not been re-audited formally
- `GL-07`: `stage10` and `stage11` are not yet compared on a fully shared
  release cohort
- `README.md`, `.gitignore`, release scripts, production pointers, rollback, and
  monitoring were still incomplete at the time of the original note

So the correct label for `v1` is:

- `freeze version`
- `closeout baseline`
- `starting point for subsequent repair`

It is not:

- `final production launch version`

## 8. Recommended Repair Order From This Point

Starting from this `v1` freeze, the recommended repair order is:

1. `GL-01`: rotate cloud credentials immediately and stop relying on exposed
   plaintext credentials
2. `GL-03`: repair repository delivery basics, including the root `README.md`
   and `.gitignore`
3. `GL-06`: rerun the formal recall audit for
   `20260311_005450_full_stage09_candidate_fusion`
4. `GL-07`: lock a shared release cohort and rerun `PreScore`, `stage10 XGB`,
   and `stage11 SFT/DPO` on the same contract
5. freeze a truly internal-trial-ready champion version afterwards

Detailed follow-up work continues primarily in:

- `docs/release/go_live_readiness_20260312.md`

## 9. Usage Rule

From this point forward, any discussion of `v1` should default to the path set
recorded in this note and in the manifest.

If a later cloud result appears:

1. do not overwrite this `v1` directly
2. first confirm that the new lineage is complete
3. then create a new standalone freeze note and a new manifest

This keeps `v1` permanently reviewable and avoids repeatedly losing the baseline
while follow-up fixes continue.
