## Archived Legacy Tests

These tests were written for the previous repository surface and pointer layout.

They are kept for local reference and diff review, but they are no longer collected by `pytest` after the current public closeout.
